# MeshBasicMaterial

- Meaning: no light reaction
- Best for: helper objects, UI shapes, simple color blocks
- Main properties: `color`, `wireframe`, `transparent`, `opacity`, `side`
- Teaching line: same object stays visible even if lights change
